# ★ This is the active folder

**Edit files in this folder only.**  
This is the canonical source for the PreBuild landing page.

- **Deploys to:** https://builder.centriweb.com
- **Run locally:** `npm run dev` (default port 5173)
- **Build for deploy:** `npm run build` → outputs to `dist/`

For full project context, see `../PROJECT-MAP.md` and `../CLAUDE.md` (repo root).
